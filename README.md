# Player And Square Screen Recorder

This repository contains sanitized source copies of two projects:

- `player`: Vite square video player. Generated demo videos are excluded; run `npm run generate:videos` after installing dependencies.
- `screeh`: Electron square screen recorder. Runtime downloads, recordings, proof artifacts, Playwright output, and logs are excluded.

Use each subproject README for setup and validation commands.
